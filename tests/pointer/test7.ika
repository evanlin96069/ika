const sz1 = sizeof(*i32);
const sz2 = sizeof([]u8);
const sz3 = sizeof(fn (a: i32, b: i32) i32);
const sz4 = sizeof(*[3]u8);
const sz5 = sizeof([][3]u8);

"%d %d %d %d %d\n", sz1, sz2, sz3, sz4, sz5;
