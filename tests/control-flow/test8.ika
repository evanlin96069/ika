// GDC(x, y)

var x: i32 = 54;
var y: i32 = 24;

while (x != 0) {
    if (x <= y) {
        var tmp: i32 = x;
        x = y;
        y = tmp;
    }

    while (x >= y) {
        x -= y;
    }

    "%d, %d\n", x, y;
}

"%d\n", y;
